{
  "name": "workman-tech",
  "version": "1.0.0",
  "private": true,
  "description": "Static site build for workman.tech — markdown posts to HTML, served by GitHub Pages.",
  "scripts": {
    "build": "node build.js",
    "clean": "node build.js --clean"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0"
  }
}
